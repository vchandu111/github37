function multiply(a, b, c) {
  return a * b * c;
}

function add(a, b) {
  return a + b;
}

export const API_KEY = "abc";

export default function divide(a, b) {
  return a / b;
}

export { multiply, add as addition };

// passwords, addresses, apiKeys, authentications, authorization tokens
